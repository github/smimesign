#!/bin/bash
set -ex

ROOTDIR="$(cd "$(dirname $0)/.." && pwd)"
IMAGE=golang:1.9-stretch
WORKDIR=/go/src/github.com/github/go

docker run --rm --volume "$ROOTDIR:$WORKDIR" --workdir "$WORKDIR" $IMAGE ./script/test
